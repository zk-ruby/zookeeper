
task :clean do
  sh 'make clean'
end

task :clobber => :clean do
  rm_rf %w[Makefile c]
end

task :build_zkc do
  ruby 'extconf.rb'
end

file 'Makefile' => :build_zkc

task :build => 'Makefile' do
  sh 'make'
end

task :default => :build

